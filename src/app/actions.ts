
'use server';

// No client-facing server actions are needed with the new API-based flow.
// The AI flow functions (identifyPokemonCard, estimateCardValue) are now
// imported and used directly by the /api/scan-card server-side route.

// If you had other utility server actions not related to the core scan/fetch process,
// they would remain here. For now, this file can be empty or removed if not used elsewhere.

export {}; // Keep if other actions might be added, or remove file if truly empty.
